import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Contentlesson6Page } from './contentlesson6.page';

describe('Contentlesson6Page', () => {
  let component: Contentlesson6Page;
  let fixture: ComponentFixture<Contentlesson6Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(Contentlesson6Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
