import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Contentlesson13Page } from './contentlesson13.page';

describe('Contentlesson13Page', () => {
  let component: Contentlesson13Page;
  let fixture: ComponentFixture<Contentlesson13Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(Contentlesson13Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
