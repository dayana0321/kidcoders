import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Contentlesson10Page } from './contentlesson10.page';

describe('Contentlesson10Page', () => {
  let component: Contentlesson10Page;
  let fixture: ComponentFixture<Contentlesson10Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(Contentlesson10Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
