import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Contentlesson14Page } from './contentlesson14.page';

describe('Contentlesson14Page', () => {
  let component: Contentlesson14Page;
  let fixture: ComponentFixture<Contentlesson14Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(Contentlesson14Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
