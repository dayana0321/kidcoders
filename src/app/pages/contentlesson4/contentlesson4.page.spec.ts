import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Contentlesson4Page } from './contentlesson4.page';

describe('Contentlesson4Page', () => {
  let component: Contentlesson4Page;
  let fixture: ComponentFixture<Contentlesson4Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(Contentlesson4Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
