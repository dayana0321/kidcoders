import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Contentlesson11Page } from './contentlesson11.page';

describe('Contentlesson11Page', () => {
  let component: Contentlesson11Page;
  let fixture: ComponentFixture<Contentlesson11Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(Contentlesson11Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
