import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Lessonscourse4Page } from './lessonscourse4.page';

describe('Lessonscourse4Page', () => {
  let component: Lessonscourse4Page;
  let fixture: ComponentFixture<Lessonscourse4Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(Lessonscourse4Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
