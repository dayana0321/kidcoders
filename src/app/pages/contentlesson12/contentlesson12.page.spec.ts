import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Contentlesson12Page } from './contentlesson12.page';

describe('Contentlesson12Page', () => {
  let component: Contentlesson12Page;
  let fixture: ComponentFixture<Contentlesson12Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(Contentlesson12Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
