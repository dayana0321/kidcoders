import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Contentlesson15Page } from './contentlesson15.page';

describe('Contentlesson15Page', () => {
  let component: Contentlesson15Page;
  let fixture: ComponentFixture<Contentlesson15Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(Contentlesson15Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
