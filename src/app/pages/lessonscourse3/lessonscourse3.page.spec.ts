import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Lessonscourse3Page } from './lessonscourse3.page';

describe('Lessonscourse3Page', () => {
  let component: Lessonscourse3Page;
  let fixture: ComponentFixture<Lessonscourse3Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(Lessonscourse3Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
