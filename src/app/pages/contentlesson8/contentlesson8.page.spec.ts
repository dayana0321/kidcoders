import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Contentlesson8Page } from './contentlesson8.page';

describe('Contentlesson8Page', () => {
  let component: Contentlesson8Page;
  let fixture: ComponentFixture<Contentlesson8Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(Contentlesson8Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
