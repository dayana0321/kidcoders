import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Contentlesson7Page } from './contentlesson7.page';

describe('Contentlesson7Page', () => {
  let component: Contentlesson7Page;
  let fixture: ComponentFixture<Contentlesson7Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(Contentlesson7Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
