import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Contentlesson9Page } from './contentlesson9.page';

describe('Contentlesson9Page', () => {
  let component: Contentlesson9Page;
  let fixture: ComponentFixture<Contentlesson9Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(Contentlesson9Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
