import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Contentlesson16Page } from './contentlesson16.page';

describe('Contentlesson16Page', () => {
  let component: Contentlesson16Page;
  let fixture: ComponentFixture<Contentlesson16Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(Contentlesson16Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
